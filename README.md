## About

It's a simple cypress project configured for chrome. 

## Getting Started

First, install the dependencies:

```bash
npm install
# or
yarn
```

Then, run cypress:

```bash
npm run cypress
# or
yarn cypress
```
